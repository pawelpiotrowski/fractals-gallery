import type { NextPage } from "next";
import MandelbrotComponent from "../components/fractals/mandelbrot/component";

const Home: NextPage = () => {
  return <MandelbrotComponent />;
};

export default Home;
